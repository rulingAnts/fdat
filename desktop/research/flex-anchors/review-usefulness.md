1. **§1.1 contradicts §3.7 on custom columns — flip the recommendation.** The table says *"Merge unit is one row's one field either way; the \"merges as one string\" worry is per-row, which is acceptable. Prefer per-column fields while columns are few and stable."* But §3.7 item 1 says definition changes are the one fragile S/R operation ("It is possible to get into a situation where some team members cannot merge their changes and must Get the project again") and tells FDAT to "create *all* FDAT fields in one idempotent step with fixed names". Per-column fields mean a custom-field definition change every time a linguist adds a column — i.e. the fragile operation fires on an ordinary user action. Change to: *"Prefer the single `FDAT_ColumnValues` String. Per-column fields are only acceptable if the column set is fixed at install time, because adding a column later is a custom-field definition change — the one S/R operation FLEx itself warns about (§3.7.1)."*

2. **Promote the TextTag rejection into §1.3.** §1.3 is headed *"Two corrections to the plan you should act on now"*, but the biggest correction to PLAN §5 — that its band mechanism (TextTags) is the wrong anchor — sits at §5.5, after five pages. A reader of §1 never learns the plan's band design is rejected. Add a third bullet to §1.3: **"Row bands do not belong in `TextTag`."** with the four operative reasons compressed to one line each (wrong anchor object; rows can cross 3+ segments where FLEx support is incomplete; visible and hand-editable in four places colleagues use daily; lands in the text-corpus sync file, not the chart file), pointing to §5.5 for detail.

3. **§1.1's marker row names no store — it isn't implementable as written.** *"**Per-marker styling / show-hide / order** | FDAT's **own** store keyed by the marker `CmPossibility` GUID"* — "FDAT's own store" is undefined in the summary table. Replace the Anchor cell with the concrete field: *"Inside `FDAT_DiscourseSettings` on `DsDiscourseData` (the same JSON as project-level settings), keyed by marker GUID"*, and add "— note this is the one unverified anchor; see the row below" so the reader sees the dependency immediately.

4. **The summary hides the severe concurrency cost of chart-object custom fields.** §9.2's *"The one severe failure mode"* — a foreign change to any object the FLEx user has unsaved edits on pops "FieldWorks cannot save your changes" and a Refresh reverts their unsaved work, and `DsConstChart.Rows` being an owning sequence makes writing `FDAT_ChartSettings` during row insertion exactly that case — is a property of the *recommended* anchors and belongs in §1.1, not buried at §9.2. Add a line under the table: *"All chart-object writes require project Sharing to be on, and writing to a `DsConstChart` or `ConstChartRow` the FLEx user has unsaved edits on can cost them that unsaved work (§9.2). Keep write windows ≤1 s and prefer rows the user is not editing."*

5. **§1.2's fact 2 cancels itself — state the real decisive reason.** Fact 2 is presented as one of *"Four hard facts decide it"* (*"**There is no merge.**"*), then three paragraphs later: *"**Per-peer filename removes fact 2 entirely**"*. So merge behaviour decides nothing. Rewrite the verdict's reasoning to the reasons that survive: the 1 MiB silent cap, the default-location-only constraint, and — the one actually missing — that per-peer files give you N copies with no defined winner, so there is no single store of record to read back. Demote the no-merge point to a note explaining why the shared-filename variant is off the table.

6. **§1.1 gives no reason for `FDAT_ChartSettings` over `DsConstChart.Description`.** The "Why" cell reads *"`DsConstChart` is the chart, per your framing"*, which is not a rationale, while §2's own table lists `DsConstChart.Name`/`.Description` as *"Free native slots, no schema change"* that FLEx *"Never read[s], display[s], edit[s] or export[s]"* and that merge per `AStr` — the same atomic granularity, with no custom-field definition to sync. Either recommend `Description` (and say what the risk is: a future FLEx could surface it) or justify the custom field explicitly. As written, the owner cannot tell which one to build.

7. **Name and reject the simplest band option.** Nothing in the report considers storing the band as a plain `String` or `Integer` code on `ConstChartRow` with the band vocabulary living in `FDAT_DiscourseSettings` — which sidesteps the whole §3.4 deletion asymmetry, the custom list, and the `DeleteCustomList` cascade (§3.7.4). Add one line to §1.1's band row: *"A plain String code would avoid list-deletion risk entirely; the list is preferred because band items carry stable GUIDs, are user-visible/renameable in Lists, and merge per element — accept the §3.7.4 cascade risk in exchange."* If that trade isn't actually worth it, change the recommendation.

8. **"FDAT enforces ≤1 member" ignores the merge behaviour you documented.** §3.4 says both machines' independent collection additions are kept with no conflict. So two colleagues each banding the same row produces a two-member collection that FDAT never created. Replace *"FDAT enforces ≤1 member"* with *"FDAT writes ≤1 member, but a concurrent S/R can produce two (both-add is kept silently, §3.4) — on load, pick deterministically (lowest item GUID), rewrite to one member, and surface the discarded value."*

9. **§1.4 has no spike for the finding the #1 recommendation rests on.** The band anchor is chosen entirely on the §3.4 ReferenceCollection-vs-ReferenceAtomic asymmetry, which is code-reading only and was itself a correction to an earlier claim. Add a numbered step (immediately after step 3): *"**Delete a band item.** With a row referencing an FDAT band item, delete that item in the Lists area. Confirm the row's `<Custom name=\"FDAT_Band\">` becomes empty rather than retaining an `<objsur>`; repeat after closing and reopening the project (values loaded from disk are not registered as incoming refs). If a dangling objsur survives, the band anchor must change."*

10. **§1.4 has no spike for §9.2's failure mode.** Add: *"**Write while FLEx is editing.** With Sharing on and FLEx open on the same chart with an unsaved row insertion, have FDAT write `FDAT_ChartSettings` on that `DsConstChart`. Confirm whether the ConflictingSave dialog appears and what the FLEx user loses. This decides whether FDAT may write chart-level fields at all while FLEx is open."*

11. **§1.4 step 8 is not a test.** *"**Row deletion.** Empty a row in FLEx's chart that carries FDAT data and confirm what you expect from §8."* Replace with the actual expectation: *"Empty a row that carries a band and a column value, with and without text in Notes. Expect: with Notes, the row survives with its GUID and FDAT data; without, the row is deleted and the FDAT data goes with it. Then trigger `RemoveMissingMarker` on a marker-only row and confirm the new GUID — the §8.3 step-4 fallback must catch it."*

12. **Bound the backup file.** §1.2 says *"Write order: LCM fields → `Save()` → then the JSON"* and §1.1 offers *"Everything, again, as backup"* with a per-peer filename. Written on every save, per peer, per chart, that is an unbounded series of ≤1 MiB revisions in a repo whose maintainers state that one large file kills it for the whole team. Add an explicit cadence and retention rule (e.g. "write at most once per session close or on explicit 'Back up now'; one file per peer per chart, overwritten, never appended") and say plainly what the backup does *not* protect against (it is itself deletable, capped, and does not sync when LinkedFiles is relocated).

13. **State a minimum viable set.** §1.1 recommends seven anchors plus a custom list plus a backup file plus two optional `StText` fields, and §1.4 spikes all of it. Add one line before the table: *"For v1, build only: the FDAT list + `FDAT_Band` (ReferenceCollection) on `ConstChartRow`, `FDAT_ColumnValues` (String) on `ConstChartRow`, and `FDAT_ChartSettings` (String) on `DsConstChart`. Everything else — `DsDiscourseData`, the `StText` prologue/epilogue fields, the JSON backup — is deferred until those three survive a two-machine round trip."* Without this the report is a menu, not a recommendation.

14. **Cut the editorial bookkeeping.** *"Everything else in this report comes from single-pass code reading and is **[unchecked]** unless marked otherwise — I flag every place a recommendation rests on unchecked material. Two prior findings were **[refuted]**…"* — if the default is unchecked, "I flag every place" promises nothing. Replace with: *"Except where marked **[confirmed]**, everything here is single-pass code reading and is unverified against a real project."* Delete the "[refuted → corrected]" tags in §3.4 and just state the facts; the owner has no use for the revision history of a draft.

15. **Fix the Scope sentence or split §7.** Scope says the report is about *"Where FDAT can put per-row data … per-marker presentation, and per-chart settings so Send/Receive carries it"*, but §7 (three pages of export-format spec and `fdat_lcm.py` line-by-line corrections) answers a different question and pushes the storage material apart. Either extend the Scope sentence to say the report also specifies the FLEx chart export format and corrects PLAN §4, or move §7 and §7.3 into their own document and leave a one-line pointer. Same for §1.4 step 6, which is an export task sitting inside a storage spike plan.

16. **§4.2 is a signpost, not a section.** *"### 4.2 Size, merge, extension — Covered in §1.2:"* restates §1.2 and adds one new fact (backup inclusion). Delete the section; move the backup sentence into §4.1.

17. **Trim §6.1.** The Notes comparison table ends at *"Both are **user prose**; FDAT should write `Notes` only when the user types it"* — which is PLAN rule 4, already accepted. Cut to two sentences (Notes is user prose and is rescued to `SavedNotes.txt`; a Segment note cannot be assumed to belong to one row) and drop the table. §6.3 can likewise end at the duplicate-`Name` trap plus its conclusion; the `StStyle` property list and factory signatures are unused by any recommendation.

18. **Dedupe §10 against §1.4.** Open questions 1, 3, 4, 6, 7, 8 restate spike steps 5, 1, —, 7, —, — in different words. Keep §10 for the things nobody can test this week (the blocked SIL documents, the undo/redo GUID question, the ForeColor sweep) and delete the rest, or make each §10 entry a cross-reference to its spike number.

19. **Add a short "Uninstall" note.** The removal path is scattered: §3.6 (`DeleteObjOwner` the owned `StText` before `DeleteCustomField`), §3.7.4 (list deletion cascades to fields), §1.3 (reset `ForeColor` to `-1073741824`). Since no FLEx UI can see or delete these fields (§1.3, §3.1), FDAT owns the whole lifecycle — collect the four steps into one three-line subsection so it can actually be implemented.